{
  "name": "macicon",
  "description": "A simple CLI tool to generate an app icon that complies with macOS' design specs.",
  "version": "0.0.69",
  "type": "module",
  "module": "cli.ts",
  "bin": {
    "macicon": "./src/index.ts"
  },
  "author": {
    "name": "Kristof Dewilde",
    "email": "code@qrivi.dev"
  },
  "scripts": {
    "dev": "bun --watch run ./src/cli.ts -- set /Applications/Cyberduck.app",
    "build": "rm -rf ./build; bun build ./src/cli.ts --compile --minify --outfile ./build/macicon && find . -name \"*.bun-build\" -delete",
    "check": "biome check ./src",
    "check:apply": "biome check --apply ./src"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.6.0",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "bun": "^1.0.30",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "jimp": "^0.22.12",
    "zod": "^3.22.4"
  }
}
